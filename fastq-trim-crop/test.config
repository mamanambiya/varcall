params{
  project_name = "project"
  sample_sheet = "/users/mamana/varcall/fastq-trim-crop/NA12878.samplesheet.tsv"
  out_dir = "/scratch3/users/mamana/varcall/fastq-trim-crop"
  trimmomatic_threads = 4 // Trimmomatic seems to use max 4 threads even is specified more than that
  crop = 150
  
}